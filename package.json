{
  "name": "retrocastle",
  "version": "1.0.0",
  "description": "",
  "main": "client/index.html",
  "scripts": {
    "install": "pnpm install && cd client && pnpm install && cd ../server && pnpm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "cd server/seeders && node seed",
    "build": "cd client && pnpm run build",
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && pnpm run watch\" \"cd client && pnpm run dev\"",
    "dev": "cd client && pnpm dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
